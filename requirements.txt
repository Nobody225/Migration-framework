# ═══════════════════════════════════════════════════════════════
# requirements.txt — Enterprise Migration Framework
# Python 3.11+ / 3.12
# ═══════════════════════════════════════════════════════════════

# ── VMware vSphere ───────────────────────────────────────────────
pyVmomi==8.0.2.0.1

# ── OpenStack SDK ────────────────────────────────────────────────
openstacksdk==3.1.0
python-keystoneclient==5.4.0
python-novaclient==18.6.0
python-neutronclient==11.2.0
python-glanceclient==4.4.0
python-cinderclient==9.4.0

# ── Configuration ────────────────────────────────────────────────
pyyaml==6.0.1
python-dotenv==1.0.1

# ── SSH / Remote execution (benchmarking) ────────────────────────
paramiko==3.4.0

# ── REST API + Dashboard ─────────────────────────────────────────
# Flask 3.x + Flask-SocketIO 5.x requires threading mode (pas eventlet)
# eventlet est incompatible avec Werkzeug >= 3.x
flask==3.0.3
flask-socketio==5.3.6
flask-cors==4.0.0
# PAS de eventlet — on utilise threading (async_mode='threading')
# python-socketio et python-engineio sont tirés par flask-socketio
python-socketio>=5.11.0
python-engineio>=4.9.0

# ── CLI ──────────────────────────────────────────────────────────
click==8.1.7
rich==13.7.1

# ── Utilities ────────────────────────────────────────────────────
requests==2.31.0
python-dateutil==2.9.0

# ── Testing ──────────────────────────────────────────────────────
pytest==8.1.1
pytest-mock==3.14.0
pytest-cov==5.0.0

# ── Sécurité ─────────────────────────────────────────────────────
PyJWT>=2.8.0
bcrypt>=4.0.0
